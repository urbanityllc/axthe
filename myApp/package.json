{
  "name": "mywebapp",
  "private": true,
  "scripts": {
    "watchNode": "npx nodemon",
    "test": "npx tape 'tests/**/*.test.js' | faucet"
  },
  "dependencies": {
    "axthe": "^0.0.30"
  },
  "devDependencies": {
    "nodemon": "^2.0.7",
	"faucet": "^0.0.1",
    "tape": "^5.2.2"
  }
}
